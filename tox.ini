[flake8]
import-order-style = google
application-import-names =
  altinity_datasets
ignore = H306

[tox]
envlist=flake8

[testenv]
deps =
    -rtest-requirements.txt

[testenv:flake8]
deps = {[testenv]deps}
commands = flake8 altinity_datasets
